
document.ready=function {
	$.ajax= ('.table table-dark.facture.table-active (cell2)'.insertCell('cell0*cell1');
		var total = cell0*cell1;
	$('#total).insertCell(2).innerHTML = cell2.val(total);
	
	$'.table table-dark.montant.table-active(cell0)'.var montant =insertCell(1).innerHTML = total;}